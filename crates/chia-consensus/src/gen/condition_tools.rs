use crate::gen::SpendBundleConditions;
use chia_bls::PublicKey;

pub fn pkm_pairs(conditions: SpendBundleConditions, additional_data: &[u8]) -> (Vec<PublicKey>, Vec<&[u8]>) {
    let pks = Vec<PublicKey>::new();
    let msg = Vec<V&[u8]>::new();
    return (pks, msgs)
}